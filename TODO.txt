# General

* create custom sgf search/view app
* create screen saver

~~~~~~~~~~~~~~~~~~~~
# Installer

* add build steps/custom target/whatever to automate rebuilding install
	package
* improve installer to allow user to decided whether to install for
	all users (as it does now), or just themselves. (into ~/Library etc.)

~~~~~~~~~~~~~~~~~~~~
# Spotlight importer

* add str coded board position attr for use by ql gen to create thumbnail
* replace parser w/simple self-contained module that doesn't require non-Xcode
	tools

~~~~~~~~~~~~~~~~~~~~~
# quicklook generator

* create quicklook thumbnail generator
* parse file if metadata not available


